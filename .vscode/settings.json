{
  "python.envFile": "${workspaceFolder}/.env",
  "python.terminal.useEnvFile": true,
  "python.analysis.exclude": [
    "references/**"
  ],
  "git.ignoredRepositories": [
    "${workspaceFolder}/references/atproto",
    "${workspaceFolder}/references/bps-website",
    "${workspaceFolder}/references/cookbook",
    "${workspaceFolder}/references/cookbook/nextjs-oauth"
  ],
  "files.watcherExclude": {
    "**/references/**": true
  }
}
